<%= form_for(@interest) do |f| %>
  <%= render "shared/error_messages", :object => @interest %>
  <%= f.hidden_field :user_id, :value => current_user.id %>
  <%= f.text_area :interest, :id=>"tags", :class=>"set-height"%>
  <br/>
  <div class="actions"> 
    <%= f.submit "Submit", :class => "btn btn-primary btn-mini"%>
  </div>
<% end %>
