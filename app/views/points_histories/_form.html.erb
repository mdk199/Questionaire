<%= form_for(@points_history) do |f| %>
  <% if @points_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@points_history.errors.count, "error") %> prohibited this points_history from being saved:</h2>

      <ul>
      <% @points_history.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :points_map_id %><br />
    <%= f.text_field :points_map_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
