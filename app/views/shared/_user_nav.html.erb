<% if current_user.present? %>
  <ul class="nav navbar-right">
    <li>
      <% path = current_user.profile.present? ? edit_profile_path(current_user.profile) : new_profile_path %>
      <a href="<%=path%>">
        Welcome 
        <%= current_user.profile ? current_user.profile.fullname : current_user.username %>
      </a>
    </li>
    <li class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown">My Account <b class="caret"></b></a>
      <ul class="dropdown-menu">
        <li>
          <%if current_user.profile.present? %>
            <%= link_to "Profile", edit_profile_path(current_user.profile) %>
          <% else %>
            <%= link_to "Profile", new_profile_path %>
          <% end %>
        </li>
        <li><a>My Points</a></li>
        <li>
          <%= link_to 'Add Expertise', new_expertise_path %>
        </li>
        <li>
          <%= link_to 'Add Interest', new_interest_path %>
        </li>
        <li class="divider"></li>
        <li>
          <%= link_to "Logout", destroy_user_session_path, :method => :delete, :role => "menuitem", :tabindex => "2" %>
      </ul>
    </li>
  </ul>
<% else %>
  <ul class="nav navbar-right">
    <li>
      <%= link_to "Sign up", new_user_registration_path %>
    </li>
    <li>
      <%= link_to "Sign in", new_user_session_path %>
    </li> 
  </ul>
<%end%>